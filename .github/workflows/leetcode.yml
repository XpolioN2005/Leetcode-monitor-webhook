name: LeetCode Monitor

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 * * * *" # Runs every hour (UTC)
  workflow_dispatch:

jobs:
  run-leetcode-monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Download previous cache (if any)
        uses: actions/download-artifact@v4
        with:
          name: leetcode-cache
          path: cache
        continue-on-error: true

      - name: List cache folder after download
        run: |
          echo "Listing files in cache folder after download:"
          ls -lah cache || echo "Cache folder does not exist"

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run LeetCode monitor
        env:
          WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
        run: |
          python main.py

      - name: Check cache file before upload
        run: |
          echo "Checking file to upload:"
          ls -lah cache/leetcode_stats_cache.json || echo "Cache file not found!"

      - name: Upload cache artifact
        uses: actions/upload-artifact@v4
        with:
          name: leetcode-cache
          path: cache/leetcode_stats_cache.json
